import styled from 'styled-components';

const ShowPasswordToggle = styled.div`
  position: relative;

  svg {
    position: absolute;
    bottom: 20px;
    right: 10px;
    font-size: 30px;
    cursor: pointer;
  }
`;
export default ShowPasswordToggle;
